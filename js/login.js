(function(){ensureDefaultSyncConfig();let isRegisterMode=false;let showPwd=false;function applyMode(){if(isRegisterMode){document.getElementById('formTitle').textContent='注册';document.getElementById('submitBtn').textContent='注册';document.getElementById('loginHeroSub').textContent='创建账号开始听写';document.getElementById('switchModeBtn').textContent='已有账号？去登录'}else{document.getElementById('formTitle').textContent='登录';document.getElementById('submitBtn').textContent='登录';document.getElementById('loginHeroSub').textContent='登录后开始听写';document.getElementById('switchModeBtn').textContent='没有账号？去注册'}}function bindLoginPage(){document.getElementById('switchModeBtn').onclick=()=>{isRegisterMode=!isRegisterMode;applyMode()};document.getElementById('eyeBtn').onclick=()=>{showPwd=!showPwd;document.getElementById('passwordInput').type=showPwd?'text':'password';document.getElementById('eyeBtn').textContent=showPwd?'👁':'🔒'};document.getElementById('submitBtn').onclick=async()=>{const name=document.getElementById('usernameInput').value;const password=document.getElementById('passwordInput').value;if(!name){showToast('请输入用户名');return}if(!password){showToast('请输入密码');return}showLoading(isRegisterMode?'注册中...':'登录中...');const result=isRegisterMode?await registerUser(name,password):await loginUser(name,password);hideLoading();if(result.ok){showToast(isRegisterMode?'注册成功':'登录成功');document.getElementById('usernameInput').value='';document.getElementById('passwordInput').value='';isRegisterMode=false;applyMode();setTimeout(()=>{if(typeof onLoginSuccess==='function')onLoginSuccess()},400)}else{showToast(result.error||'操作失败')}};document.getElementById('passwordInput').addEventListener('keydown',(e)=>{if(e.key==='Enter')document.getElementById('submitBtn').click()});document.getElementById('usernameInput').addEventListener('keydown',(e)=>{if(e.key==='Enter')document.getElementById('passwordInput').focus()})}window.addEventListener('DOMContentLoaded',bindLoginPage)})();